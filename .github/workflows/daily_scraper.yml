name: Daily Job Scraper

on:
  schedule:
    - cron: "30 14 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          pip install pandas requests python-jobspy lxml openpyxl

      - name: Run Scraper
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASS: ${{ secrets.GMAIL_PASS }}
          TO_EMAIL: ${{ secrets.TO_EMAIL }}
        run: python backend/main.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "MaSudhan Bot"
          git config --global user.email "bot@masudhan.com"
          
          # 1. Stage the files
          git add data/jobs.json frontend/public/data/jobs.json
          
          # 2. Save (Commit) them locally FIRST
          git commit -m "Auto-update jobs [skip ci]" || echo "No changes to commit"
          
          # 3. NOW it is safe to pull updates
          git pull --rebase origin main
          
          # 4. Finally push
          git push
